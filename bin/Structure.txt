Backend
randomizer function returns a random color selection initially hidden
--method that retrieves a random color
--method that changes the color of the image according to the returned color
--method that displays the colored image 

main/guessing function returns boolean according to user selection and conditional hints
--method that compares user selection to random generated color and returns the boolean
---if attempts are more than two give hint 

hint function returns hint according to the color randomly generated
--method that returns a hint conditioned by the color

Psuedocode: 
I should have a list of color objects with unique hints. iterate through this list and randomly select a color.
a function takes user inpute and compares it to the generated color and returns try again if false, if more than two incorrect attempts have been made,
returns the hint. if match returns congratulations and game resets.